import TextAreaBase from "@components/elements/TextArea";
import Field from "./Field";

const TextArea = (props) => {
  return <Field component={TextAreaBase} {...props} />;
};

export default TextArea;
